name: HelloWorldWorkflow
on:
  push:
    branches:
      - main

jobs:
  HelloWorldJob:
    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: echo "Hello World!"

    - name: Say Goodbye
      run: echo "Goodbye World!"
